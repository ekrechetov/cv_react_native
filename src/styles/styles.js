import { Colors } from './'

export const container = {
  flex: 1,
  alignItems: 'center',
  backgroundColor: Colors.teal,
}

export const content = {
  flex: 1,
  width: '100%',
  justifyContent: 'space-between',
  paddingLeft: 20,
  paddingRight: 20,
  paddingBottom: 26,
  maxWidth: 600,
}